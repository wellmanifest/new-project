# Ticket Changelog (ticket-059)

## [0.1.0] - 2026-08-11

- Initial governance scaffold created.
- No human participant identity or content was generated.
- Bound the compose-root detection regression to candidate `503cefa` and the
  real `mcp` monorepo layout.
- Limited the repair to explicit root markers, manifest variants and focused
  validator fixtures; recursive auto-discovery remains out of scope.
- Added Compose `.yml`/`.yaml` root markers and default manifest variants with
  positive and negative validation fixtures in `e5d67c0`.
- Passed focused/full Linux contracts and exact-SHA `mcp` adoption; the false
  stack error is gone while real mutable-image debt remains fail-closed.
- Marked the standard ticket `DONE / DONE`; no external delivery occurred.
