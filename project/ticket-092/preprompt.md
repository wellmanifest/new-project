# ticket-092 preprompt

Install a proactive worktree overlap guard without rewriting package-manifest
or governance_check.py. Reuse git identity grouping already used by
workspace_lifecycle_check.py. Trigger like pyqual.yaml.
