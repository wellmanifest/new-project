{
  "schema": "new-project.intent/v3",
  "ticket": "ticket-213",
  "summary": "Preserve active exact ticket refs without changing the conservative activity default",
  "workstream": "governance",
  "classification": {
    "kind": "BUG",
    "priority": "P1",
    "origin": "health"
  },
  "allowedPaths": [
    "governance/ticket-activity.json",
    "scripts/ticket_activity.py",
    "tests/ticket-activity.test.sh",
    "project/ticket-213/**",
    "TODO.md",
    "project/TICKETS.md"
  ],
  "forbiddenPaths": [
    "project/ticket-*/user-*.md"
  ],
  "stacks": [],
  "dependsOn": [],
  "conflictsWith": [],
  "integrationTicket": null,
  "delivery": {
    "acceptedBaseSha": "8d86cd61404d51809532a8292ce9c0cc02ed6157",
    "targetBranch": "main",
    "outcome": "Reconcile the closed unmerged ticket losslessly: retain main's status-projection default and explicit target-owned opt-in, while preserving unfinished exact, dashed and nested ticket refs in Git-derived activity resolution.",
    "nonGoals": [
      "Do not weaken receipt validation or rewrite the append-only terminal receipt registry.",
      "Do not restore the global git-ancestry default rejected for a safety regression in PR 322.",
      "Do not change the explicit status-projection option for adopters that retain it.",
      "Do not infer terminal state from an unmerged ticket branch or from carrier-only changes."
    ],
    "complexity": "S",
    "estimatedMinutes": 25,
    "budgets": {
      "maxImplementationFiles": 3,
      "maxAffectedComponents": 2,
      "maxPublicInterfaceChanges": 1,
      "maxRuntimeDependencies": 0
    },
    "architecture": {
      "status": "accepted",
      "decision": "Merge the current target without rewriting the preserved three-commit history. Supersede the rejected global-default change with the current target-owned opt-in boundary. Retain only the exact-ref safety correction and regression tests within the existing three implementation paths and file budget.",
      "components": [
        {
          "name": "ticket-activity-default",
          "paths": ["governance/ticket-activity.json", "scripts/ticket_activity.py", "tests/ticket-activity.test.sh"]
        }
      ],
      "responsibilityChanges": false,
      "interfaceChanges": [],
      "dataChanges": [],
      "ui": {"impact": "none", "states": [], "evidence": []},
      "rollback": "Revert the exact-ref correction through protected delivery; never change the default or the target-owned override as part of rollback."
    },
    "runtimeDependencies": [],
    "validation": [
      {
        "criterion": "AC-01",
        "commands": ["bash tests/ticket-activity.test.sh"],
        "evidence": "The missing-registry default remains conservative; the current main target-owned override and invalid-override rejection remain unchanged."
      },
      {
        "criterion": "AC-02",
        "commands": ["bash tests/ticket-activity.test.sh", "python3 tests/work_start_test.py", "bash tests/governance-validator.test.sh"],
        "evidence": "Local and fetched exact/dashed/nested refs keep unfinished tickets active; adjacent ticket numbers and integrated refs do not reserve that scope. The declared CI suites must pass before protected merge."
      }
    ]
  }
}
