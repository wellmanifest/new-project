# Changelog

- Reproduced ignored managed payload and shallow GitHub pytest lifecycle gaps
  from the semcod/koru adoption pilot.
- Added a mutation-free ignored-target precondition to the immutable adoption
  generator.
- Bound pytest startup to the installed hook contract and exact GitHub event
  base SHA, including a real depth-one clone regression.
- Passed Ruff, compileall, focused regressions, the governance validator and
  the complete deterministic Linux shell suite.
