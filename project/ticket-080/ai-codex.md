---
participant-id: agent:codex
participant: codex
role: agent
ticket: ticket-080
---
# Participant: codex (AI agent)

## Understanding

The requested outcome is not only a second parser repository. Policy DSL must
become a reproducible dependency of the governance standard and therefore of
every repository that adopts `wellmanifest/new-project`. A target containing
`CONTRIBUTING.md` must validate all selected policy fragments using the exact
Policy DSL runtime before the existing evaluator or enforcement audit runs.

The dependency must be reachable, reviewed and pinned by full Git SHA plus
content digest. Runtime validation must work offline after adoption and must
not import `wellm`, compile language-specific expressions, execute actions or
interpret Bash examples as policy.

The user's repeated request to continue autonomously records
`SESSION_EXECUTION_AUTHORIZATION` for implementation and protected delivery.
It does not replace the independent exact-head approval required for the
Policy DSL dependency or this standard's eventual merge.

## Execution plan

1. Wait for independent exact-head review of `wellmanifest/policy-dsl#1`.
2. Return this ticket to `IN_PROGRESS / EDIT` without changing its scope.
3. Vendor the reviewed dependency-free checker and create its closed lock.
4. Add both artifacts to the immutable governance adoption package.
5. Invoke Policy DSL from the governance gate, runtime policy command and rule
   enforcement audit, failing closed on missing, changed or invalid runtime.
6. Normalize the one legacy policy-shaped Bash fence to canonical `dsl` and
   shorten the single Policy opcode that exceeds the reviewed v1 bound.
7. Reuse the existing generic package, adoption and rule-enforcement
   regressions, which automatically cover every managed package entry.
8. Run focused and full gates, then publish through exact-head review. Allocate
   a separate bounded release ticket only after the feature is integrated.

## Actual changes

- Allocated ticket 080 only through the managed allocator from
  `origin/main@7089c4a9a0834c08539d4d6f9785118345e4cc36`.
- Recorded the exact public Policy DSL candidate revision and bounded the
  target-owned adoption scope before implementation.
- Released the governance workstream by setting `PLAN / PLAN` until the
  independent dependency review exists.
- Verified trusted Validator App approval for Policy DSL exact head
  `daaf7b7b96312a2469de1b4799f2f81c7396de4e`, protected merge
  `6fe12f9fcce206c1d69b95f9cac1b4bc8c10abbf` and deletion of the ticket branch.
- Returned this ticket to `IN_PROGRESS / EDIT` before changing implementation
  files.
- Reconciled the slice with the authoritative hub delivery profile before an
  implementation commit: complexity `S`, at most 9 implementation files, 3
  components and zero install-time dependencies. The reviewed checker is
  vendored data with immutable provenance, not a package-manager dependency.
  Versioning and release metadata move to a dependent publication ticket after
  this feature is integrated.

## Blockers

- None. The external Policy DSL publication prerequisite is satisfied.
