---
participant-id: agent:codex
participant: codex
role: agent
ticket: ticket-098
---
# Participant: codex (AI agent)

## Understanding

Core integrated the authoritative full hook, then its mandatory ticket020
closure could not be committed: the hook accepts only `IN_PROGRESS`, while
policy requires `DONE / DONE` to be written only after trusted merge. Using
`--no-verify` would turn the standard itself into an operational bypass.

## Execution plan

1. Commit this accepted-base plan before changing the hook or tests.
2. Add closure fixtures proving current rejection and staged-status behavior.
3. Implement an index-bound allowlist for terminal governance-only closure.
4. Run focused and full hub suites, syntax and exact-base governance.
5. Publish through exact-head Validator, then adopt the merged revision in
   Core before closing its integrated ticket020.

## Actual changes

- Initialized the bounded ticket and recorded `SESSION_EXECUTION_AUTHORIZATION`
  from the request to execute this work.
- Bound the regression to integrated standard base
  `36071e6e90636b464725af1c07718572edd0bf3d` and the reproduced Core closure
  failure.
- Changed ticket status authority from the working tree to Git's staged
  snapshot, closing the mismatch bypass.
- Added a narrow `DONE` closure mode: the ticket README must be staged and
  every staged path must be an addition/modification under that ticket, TODO or
  the ticket index; deletions, renames, copies and every other path fail closed.
- Bash syntax, focused host fixtures and all eleven Linux hub suites pass.
- GitHub `test` and `windows-governance` checks passed at exact head
  `ac711a512a9b8078e044d33886559c548b9ee80c`.
- Independent Validator approval `4988534590` accepted that exact head and
  merged PR #158 as `b4fbf6bd5aa9218815191511e827db95b2fc5b74`.

## Blockers

- None inside the recorded intent; proceed without a second confirmation.
- New authority is still required for destructive action, secret access, new
  external coordination or material objective expansion. Protected delivery
  may be invoked without another prompt when publication is in scope; its
  exact-head trusted approval remains independent evidence.
