# Version

## Current Version

`0.9.0`

## Version Type

Minor planner-feature release over `0.8.0`.

This version implements Phase 5 bidirectional NL <-> DSL support for the deterministic, mock-safe Intent/Contract planner boundary. `@office-dsl/llm-planner` now defines a controlled `intent-contract.planner-response.v1` response schema for OpenRouter-style output, validates malformed responses before DSL creation, converts valid structured responses into canonical `intent-contract.dsl.v1`, plans varied single-message natural language and guideline-file text into sourced DSL fields, renders DSL back to traceable natural-language summary statements, and includes round-trip regression checks that prevent rendered NL from introducing unauthorized field paths.

The Phase 5 implementation is validated offline. It does not call OpenRouter during default verification and does not claim production semantic extraction from arbitrary documents; the controlled schema and prompt boundary are ready for an online provider to use behind explicit configuration.

This version also implements Phase 6 contract and legal document renderers in the new `@office-dsl/document-renderer` package. The renderers turn an `intent-contract.dsl.v1` snapshot into task delegation, service agreement, and employment/guideline draft documents, embed a draft legal disclaimer in every output, render missing/ambiguous/conflicting/unapproved fields as explicit gaps, and attach a document-to-DSL traceability map. The renderers are deterministic and read-only over the DSL and never invent terms. Wiring them into the runtime, CLI, backend, and UI, and gating rendering behind bilateral canonical approval, remains open.

This version also implements Phase 8 test generation in the new `@office-dsl/testgen` package. It defines typed `intent-contract.testgen-input.v1` inputs (`REQUIREMENT`, `INVARIANT`, `ACCEPTANCE_CRITERIA`, `PROHIBITED_BEHAVIOR`, `EXPECTED_OUTPUT`, `ERROR_HANDLING`, `SECURITY_POLICY`), extracts them deterministically from an Intent/Contract DSL snapshot, generates unit/integration/API/E2E/security/error-handling test specifications mapped back to DSL inputs, and verifies coverage against acceptance criteria with uncovered criteria surfaced in the verifier input. The generator is deterministic and read-only over the DSL; it does not execute generated code and does not create tests from loose prompt text. Runtime and Python-verifier integration remain open.

## Project Status

The repository contains a working offline Office DSL MVP, repeatable office/chat/recruitment example runners, a standalone canonical Intent/Contract DSL model package, an Office DSL to Intent/Contract migration path, minimal runtime canonical approval records, and deterministic NL <-> DSL planner/rendering utilities for selected Intent/Contract fixtures.

Current validated implementation scope:

- single-request Office DSL planning through mock planner,
- `office.dsl.v1` JSON validation,
- TypeScript runtime orchestration,
- deterministic policy checks,
- simple clarification questions,
- one-side Office action confirmation with plan hash,
- per-session canonical Intent/Contract DSL snapshot hash,
- minimal Human1/Human2 runtime approval records with party, hash, timestamp, verdict, active/invalidated status, and invalidation reason,
- bilateral current-hash approval detection in runtime APIs,
- approval invalidation after canonical DSL snapshot changes,
- mock dry-run execution,
- audit output with canonical approval metadata,
- CLI, backend API, static web demo for the current Office DSL flow,
- Python mock verifier package,
- canonical office example manifests with `in/` and `out/` folders,
- deterministic single-example and all-example runner with generated `*.dsl.hcl` artifacts beside each `examples/<scenario>/`,
- executable `examples-chat` scenarios for Human1/Human2 negotiation regression,
- executable `examples-recruitment` multi-candidate recruitment fixture with deterministic Markdown/PDF/OCR ingestion, per-candidate proposals, accepted/rejected outcomes, and chat-runner negotiation reuse,
- controlled `intent-contract.planner-response.v1` schema and prompt boundary for OpenRouter-style NL-to-DSL output,
- deterministic single-message NL and guideline-file planning into canonical Intent/Contract DSL with source references and explicit missing fields,
- traceable DSL-to-NL summary rendering with statement-to-DSL path mapping,
- round-trip NL <-> DSL regression checks for selected fixtures,
- root verification command,
- GitHub Actions workflow for `project.sh install` and `project.sh verify` on Windows and Linux,
- `intent-contract.dsl.v1` model package with formal fields, statuses, source references, canonical serialization, and stable hashing,
- `intent-contract.conversation.v1` model package input format with Human1/Human2/system messages, validation, parsing, and source-reference conversion,
- deterministic mock conversation-history planner that produces partial `intent-contract.dsl.v1` with sources, unresolved fields, and blocking questions,
- rejected-approval diagnosis that blocks finalization and routes Human2 insufficient-detail rejections back to Human1 as reopened clarification questions,
- deterministic `officeDslToIntentContractDsl` compatibility adapter with migration notes,
- expanded regression coverage for DSL validation, runtime controls, security policy, canonical approval records, CLI workflows, backend API workflows, file-backed store behavior, example runners, chat/recruitment negotiation behavior, and NL <-> DSL round trips,
- documented Codex Windows sandbox `spawn EPERM` limitation for Vitest/Vite startup,
- TypeScript and Python tests for the current scope.

Target scope documented but not implemented:

- CLI/backend/web exposure for canonical Intent/Contract approval flows,
- production Human1/Human2 conversation workflow beyond typed input validation, deterministic mock fixture planning, model-layer rejection diagnosis, and deterministic recruitment fixtures,
- production OpenRouter NL/guideline-file planner validation against live provider output,
- production OCR/PDF provider integration beyond deterministic fixture extraction,
- runtime population of field-level source traceability from live planner sessions,
- contract and legal document renderers,
- JS/Node.js code generation,
- DSL-based test generation,
- TypeScript runtime integration with the Python verifier as a normal gating step.

## Included In 0.9.0

- Added controlled NL-to-DSL planner schema, validation, and OpenRouter prompt boundary.
- Added deterministic single-message and guideline-file Intent/Contract DSL planners.
- Added traceable DSL-to-NL summary renderer and rendered statement path extraction.
- Added NL <-> DSL round-trip regression tests.
- Updated root package, llm-planner package, changelog, docs, and documentation consistency tests to `0.9.0`.

## Not Included In 0.9.0

- No default online OpenRouter execution or live provider validation.
- No production semantic verifier for arbitrary NL-to-DSL equivalence.
- No CLI/backend/web surface for the new canonical NL <-> DSL planner utilities.
- No legal contract renderer beyond existing deterministic example artifacts.
- No JS/Node.js code generator.
- No DSL-based test generator.

## Runtime Compatibility Notes

Declared by the repository:

- pnpm: `9.12.0` through `packageManager`.
- TypeScript: `5.7.3`.
- Vitest: `3.0.4`.
- Python verifier: `>=3.11`.
- CI Node.js: `22`.
- CI Python: `3.11`.

Validated in this pass:

- TypeScript tests including `tests/nl-dsl-roundtrip.test.ts`.
- Existing Office, chat, and recruitment example runners through the root verification command.
- Python verifier tests through the root verification command.

Expected but not yet fully validated as release criteria:

- Online OpenRouter planner mode.
- Online LiteLLM/OpenRouter verifier mode.

## Versioning Policy Note

Root package metadata, the llm-planner package, `VERSION`, `CHANGELOG.md`, and documentation consistency tests are updated together for this milestone.

A future production hardening task should define how all package, app, verifier, and OpenAPI version metadata are kept in sync when a broader release touches those surfaces.